#标定棋盘大小, 行, 列,  指内角点个数，非棋盘格
calibration_size = (9, 6)

#采集标定图像存储路径
save_path = '../images3/'

#标定参数存储路径
calibration_param_path = './tool/calibration_param'
calibration_param_head_path = './tool/calibration_param_head'